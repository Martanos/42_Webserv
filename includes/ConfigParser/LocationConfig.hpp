#ifndef LOCATIONCONFIG_HPP
#define LOCATIONCONFIG_HPP

#include "../../includes/Core/Location.hpp"

// LocationConfig is an alias for Location class
// This maintains compatibility with existing code that expects LocationConfig
typedef Location LocationConfig;

#endif /* LOCATIONCONFIG_HPP */